cmake_minimum_required(VERSION 2.8)

ADD_DEFINITIONS(
    -std=c++11 # Or -std=c++0x
    # Other flags
)



#set(pivot_index pivot_index.cpp )
#add_executable(pivot_index ${pivot_index} )


#set(vectors vectors.cpp )
#add_executable(vectors ${vectors} )


#set(largest_number largest_number.cpp )
#add_executable(largest_number ${largest_number} )

#set(plus_one plus_one.cpp )
#add_executable(plus_one ${plus_one} )

#set(remove_element remove_element.cpp )
#add_executable(remove_element ${remove_element} )

#set(contains_duplicate contains_duplicate.cpp )
#add_executable(contains_duplicate ${contains_duplicate} )

set(reverse reverse.cpp )
add_executable(reverse ${reverse} )
